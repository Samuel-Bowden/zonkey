(ERROR) Cannot use operator '+' on two values of different types.
	2 | 	let a = 10 + 10 <+> "String";
        Left expression evaluates to type Integer, while the right expression evaluates to type String.

(ERROR) Cannot use operator '+' on two values of different types.
	4 | 	let b = (16 <+> 15.0 + (43 + 21.0) - (123));
        Left expression evaluates to type Integer, while the right expression evaluates to type Float.

(ABORTING) Cannot start execution of script due to 2 error(s).
