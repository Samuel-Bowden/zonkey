#!/usr/bin/env zonkey

# Test that unix shebangs work correctly

start {
	cli::println("First 10 elements of Fibonacci Sequence: ");

	Integer n1 = 0;
	Integer n2 = 1;

	for (Integer i = 0, i < 10, i += 1) {
		cli::print(n1);
		Integer nth = n1 + n2;
		n1 = n2;
		n2 = nth;

		if (i != 9) {
			cli::print(", ");
		}
	}

	cli::println("");
}
