function factorial(Integer n) -> Integer {
	if (n == 1) {
		return 1;
	} else {
		return n * factorial(n - 1);
	}
}

start {
	let n = Integer cli:prompt("Enter an integer n:");

	cli:println("The factorial of n is " + (String factorial(n)));
}
