class Pet {
	Integer age;

	method print_age() {
		println(self.age);
	}

	method get_age_offset(Integer offset) -> Integer {
		return self.age + offset;
	}
}

start {
	let dog = new Pet;

	dog.print_age();
}
