# Loop should count from 0-9, but skip 5

start {
	Integer count = 0;

	loop {
		if (count == 10) {
	    		break;
	  	} else if (count == 5) {
	    		continue;
	  	} else {
	    		printLineInteger(count);
	  	}

	  	count += 1;
	}
}
