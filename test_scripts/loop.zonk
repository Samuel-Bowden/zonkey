# Loop should count from 0-9, but skip 5

start {
	let count = 0;

	loop {
	  	count += 1;

		if (count == 10) {
	    		break;
	  	} else if (count == 5) {
	    		continue;
				cli::println("This should never be printed nor should the variable be created");
				let a = 5930;
	  	} else {
	    		cli::println(count);
	  	}
	}
}
