# An example of zonkey preventing type errors before execution

function num_plus_one(Integer a) -> Integer {
	return a + 1;
}

start {
	let a = 0;

	if (num_plus_one(a)) {
		println("Successful");
	}
}
