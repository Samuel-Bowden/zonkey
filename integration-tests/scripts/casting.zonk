start {
	let integer = 1;
	# Integer to String
	println("Integer: " + integer_to_string(integer));

	# Integer to Float
	println("Integer: " + integer_to_float(a));

	# String to Integer
	let integer_string = "1";
	integer = string_to_integer(integer_string);
	integer += 1;

	println("Integer: " + integer_to_string(integer));

	# String to Float
}
